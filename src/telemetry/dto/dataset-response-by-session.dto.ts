import { ApiProperty } from '@nestjs/swagger';
import { IsOptional, IsString } from 'class-validator';

// Runtime response is generated by TelemetryService.
// This DTO is only for documentation / optional runtime validation.

export class DatasetSessionSummaryDto {
  @ApiProperty({ example: '6a60f6f1-b97e-4906-b947-03ee8d7ac90d' })
  @IsString()
  sessionId!: string;

  @ApiProperty({ example: '2026-06-05T10:00:00.000Z' })
  @IsString()
  generatedAt!: string;

  @ApiProperty({
    example: {
      totalEvents: 120,
      keyboardEvents: 50,
      mouseEvents: 40,
      scrollEvents: 20,
      touchEvents: 10,
      idleEvents: 0,
    },
  })
  summary!: {
    totalEvents: number;
    keyboardEvents: number;
    mouseEvents: number;
    scrollEvents: number;
    touchEvents: number;
    idleEvents: number;
  };

  @ApiProperty({ required: false, example: [] })
  @IsOptional()
  includeEvents?: boolean;

  @ApiProperty({ required: false, example: [] })
  @IsOptional()
  events?: unknown[];
}

export class DatasetResponseBySessionDto {
  @ApiProperty({ example: 'analyst@example.com' })
  @IsString()
  userId!: string;

  @ApiProperty({ example: { totalSessions: 3 } })
  meta!: {
    totalSessions: number;
  };

  @ApiProperty({ example: [] })
  sessions!: DatasetSessionSummaryDto[];
}
